#!/bin/bash
# ==============================================================================
# 🚀 MAX RADIUS - التشغيل التلقائي عند أول إقلاع للنظام (First Boot Wizard)
# ==============================================================================

if [ -f /etc/max_radius_configured ]; then
    exit 0
fi

# تشغيل أداة maxip تفاعلياً على الشاشة الرئيسية (tty1)
/usr/local/bin/maxip

# وسم النظام بأنه تم ضبطه بنجاح لعدم تكرار الإطلاق
touch /etc/max_radius_configured
